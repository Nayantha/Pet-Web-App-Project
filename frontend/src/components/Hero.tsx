import '../assets/Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero-content">
                <h1>Welcome to Our Site</h1>
                <p>This is the hero section that stretches across the entire viewport.</p>
            </div>
        </section>
    );
};

export default Hero;
