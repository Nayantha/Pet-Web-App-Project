// this interface holds data / record from pet adoption table
interface AdoptedData {
    id: string;
    pet: string;
    updated: Date;
    created: Date;
    user: string;
    verified: boolean;
}
