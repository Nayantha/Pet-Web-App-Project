export default interface Pet {
    id: string;
    intakeReason: string;
    intakeDate: string;
    breed: string;
    baseColor: string;
    species: string;
    gender: string;
    name: string;
    adopted: boolean;
}